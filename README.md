# the-snap-sideloader-repo-template
Template for creating repositories compatible with TSS format.  
The template consists of a SQLite database schema, which you can use to create a repository file readable by TSS (or other programs that are compatible with this format).
